@{
  Layout = "_Layout";
}

@using Salon.Models
@model Salon.Models.Stylist

<h2>Stylist Details</h2>
<hr />
<h3>Name: @Html.DisplayFor(model => model.StylistName)</h3>

<ul>
  @foreach (Client client in Model.Clients)
  {
    <li>@Html.ActionLink($"{client.ClientName}", "Details", "Clients", new { id = client.ClientId})</li>
  }
</ul>

<p>@Html.ActionLink("Edit Stylist", "Edit", new { id = Model.StylistId })</p>
<p>@Html.ActionLink("Delete Stylist", "Delete", new { id = Model.StylistId })</p>
<p>@Html.ActionLink("Back to Stylist List", "StylistIndex")</p>